{
  "name": "cosd_2.0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel sync wpstart",
    "wpstart": "wp-scripts start",
    "build": "npm-run-all --parallel wpbuild",
    "wpbuild": "wp-scripts build",
    "sync": "browser-sync start -p 'cosd20.local' --files '**/*.php' 'build/*.js' 'build/*.css'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^28.2.0",
    "bootstrap": "~5.2.3",
    "bootstrap4": "npm:bootstrap@^4.6.2",
    "browser-sync": "^3.0.2",
    "normalize.css": "^8.0.1",
    "npm-run-all": "^4.1.5"
  }
}
